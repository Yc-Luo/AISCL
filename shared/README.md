# Shared Code

This directory contains shared type definitions and schemas that can be used by both frontend and backend.

## Structure

- `types/` - TypeScript type definitions
- `schemas/` - JSON schemas for validation

